# Todo Tags 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 让 LLM 在 assistant 文本中通过 `<todo:*>` 标签直接操作 todo 状态，省去独立工具调用轮次。

**Architecture:** Tag parser 作为格式转换层，复用 todo.ts 的 `applyParams` 状态机。在 `persistAssistantAndTools` 之后注入 `processTodoTags`：解析标签 → seq 解析 → 调用 applyParams → 更新 `state.todoPhases` → yield `todo_update` 事件。错误和 view 操作通过 steering queue 反馈给 LLM；前端手动修改也通过 steering 通知 LLM。seq 仅出现在 LLM 可见的 `formatSummary` 输出中，前端 UI 渲染不含 seq（前端从 metadata.phases 渲染，与 output text 无关）。

**Tech Stack:** TypeScript, Vitest, Hono, React Query

---

## File Structure

| File | Responsibility |
|------|---------------|
| **Create** `src/core/todo-tags.ts` | Tag parser、seq resolver、tag→TodoInput 转换、`applyTodoTags` 主入口 |
| **Create** `src/core/todo-tags.test.ts` | Parser + resolver + applyTodoTags 单测 |
| **Modify** `src/tools/builtin/todo.ts` | 导出 `applyParams`；`formatSummary` 加 seq 前缀；导出 `formatSummary` |
| **Modify** `src/shared/types/agent.ts` | AgentEvent 联合类型加 `todo_update` |
| **Modify** `src/core/loop.ts` | 新增 `processTodoTags` 生成器，在 `persistAssistantAndTools` 之后调用 |
| **Modify** `src/server/routes/todo.ts` | POST 成功后注入 steering |
| **Modify** `src/web/hooks/useChat.ts` | `reduceChatEvent` 处理 `todo_update` 事件 |
| **Modify** `src/core/prompt-registry.ts` | EXECUTION_WORKFLOW 加 tag 语法文档 |

---

## Task 1: todo.ts — 导出 applyParams + formatSummary 加 seq

**Files:**
- Modify: `src/tools/builtin/todo.ts`
- Test: `src/tools/builtin/todo.test.ts`

- [ ] **Step 1: 写 seq 显示的失败测试**

在 `src/tools/builtin/todo.test.ts` 的最后一个 `it` 块之后（仍在顶层 `describe('todoTool')` 内）添加：

```typescript
  // ── seq in formatSummary (for tag-based todo operations) ──

  it('formatSummary includes seq numbers for LLM consumption', async () => {
    const ctx = makeCtx()
    await initPhases(ctx, [
      { phase: 'Setup', items: ['Task A', 'Task B'] },
      { phase: 'Impl', items: ['Task C'] },
    ])
    const result = await todoTool.execute({ op: 'view' }, ctx)
    expect(result._tag).toBe('success')
    if (result._tag !== 'success') return
    // seq format: phaseIdx-taskIdx (1-based)
    expect(result.output).toContain('1-1: Task A')
    expect(result.output).toContain('1-2: Task B')
    expect(result.output).toContain('2-1: Task C')
  })
```

- [ ] **Step 2: 运行测试验证失败**

Run: `pnpm vitest run src/tools/builtin/todo.test.ts -t "seq numbers"`
Expected: FAIL — output 不含 "1-1:" 前缀

- [ ] **Step 3: 导出 applyParams**

在 `src/tools/builtin/todo.ts` 中，找到 `function applyParams` 定义处，将 `function` 改为 `export function`：

```typescript
/** Apply a single todo op to existing phases. Returns new phases + errors. */
export function applyParams(
  phases: TodoPhase[],
  params: TodoInput,
): { phases: TodoPhase[]; errors: string[] } {
```

同时在文件末尾的 export 语句中添加 `applyParams`：

```typescript
export { applyParams }
export type { TodoInput, TodoItem, TodoPhase, TodoStatus }
```

- [ ] **Step 4: 导出 formatSummary**

找到 `function formatSummary` 定义处，将 `function` 改为 `export function`：

```typescript
export function formatSummary(phases: TodoPhase[], errors: string[], readOnly = false): string {
```

- [ ] **Step 5: 修改 formatSummary 加入 seq**

在 `formatSummary` 函数体中，找到构建 `remainingByPhase` 的代码段：

```typescript
  const remainingByPhase = phases
    .map((phase) => ({
      name: phase.name,
      tasks: phase.tasks.filter(
        (task) => task.status === 'pending' || task.status === 'in_progress',
      ),
    }))
    .filter((phase) => phase.tasks.length > 0)
  const remainingTasks = remainingByPhase.flatMap((phase) =>
    phase.tasks.map((task) => ({ ...task, phase: phase.name })),
  )
```

替换为携带 seq 的版本：

```typescript
  const remainingByPhase = phases
    .map((phase, pi) => ({
      name: phase.name,
      tasks: phase.tasks
        .map((task, ti) => ({ task, seq: `${pi + 1}-${ti + 1}` }))
        .filter(({ task }) => task.status === 'pending' || task.status === 'in_progress'),
    }))
    .filter((phase) => phase.tasks.length > 0)
  const remainingTasks = remainingByPhase.flatMap((phase) =>
    phase.tasks.map(({ task, seq }) => ({ ...task, seq, phase: phase.name })),
  )
```

然后找到 remaining items 渲染处：

```typescript
    for (const task of remainingTasks) {
      lines.push(`  - ${task.content} [${task.status}] (${task.phase})`)
    }
```

替换为：

```typescript
    for (const task of remainingTasks) {
      lines.push(`  - ${task.seq}: ${task.content} [${task.status}] (${task.phase})`)
    }
```

最后找到 phase/task 全量渲染处：

```typescript
  for (const phase of phases) {
    lines.push(`  ${phase.name}:`)
    for (const task of phase.tasks) {
      const checkbox = task.status === 'completed' ? '[X]' : '[ ]'
      const tag =
        task.status === 'in_progress'
          ? ' (in progress)'
          : task.status === 'abandoned'
            ? ' (dropped)'
            : ''
      lines.push(`    - ${checkbox} ${task.content}${tag}`)
    }
  }
```

替换为：

```typescript
  for (let pi = 0; pi < phases.length; pi++) {
    const phase = phases[pi]!
    lines.push(`  ${phase.name}:`)
    for (let ti = 0; ti < phase.tasks.length; ti++) {
      const task = phase.tasks[ti]!
      const seq = `${pi + 1}-${ti + 1}`
      const checkbox = task.status === 'completed' ? '[X]' : '[ ]'
      const tag =
        task.status === 'in_progress'
          ? ' (in progress)'
          : task.status === 'abandoned'
            ? ' (dropped)'
            : ''
      lines.push(`    - ${checkbox} ${seq}: ${task.content}${tag}`)
    }
  }
```

- [ ] **Step 6: 运行测试验证通过**

Run: `pnpm vitest run src/tools/builtin/todo.test.ts`
Expected: PASS — 全部测试通过（包括新 seq 测试和原有测试）

- [ ] **Step 7: 运行类型检查**

Run: `pnpm tsc --noEmit`
Expected: 无错误

- [ ] **Step 8: Commit**

```bash
git add src/tools/builtin/todo.ts src/tools/builtin/todo.test.ts
git commit -m "feat(todo): export applyParams, add seq numbers to formatSummary"
```

---

## Task 2: 创建 todo-tags.ts — tag parser + resolver

**Files:**
- Create: `src/core/todo-tags.ts`
- Test: `src/core/todo-tags.test.ts`

- [ ] **Step 1: 写 parser 失败测试**

创建 `src/core/todo-tags.test.ts`：

```typescript
import { describe, expect, it } from 'vitest'
import type { TodoPhase } from '../tools/builtin/todo.js'
import { applyTodoTags, parseTodoTags, resolveSeq } from './todo-tags.js'

// ── Test fixtures ──

const PHASES: TodoPhase[] = [
  {
    name: 'Setup',
    tasks: [
      { content: 'Install deps', status: 'completed' },
      { content: 'Configure DB', status: 'in_progress' },
      { content: 'Write tests', status: 'pending' },
    ],
  },
  {
    name: 'Impl',
    tasks: [{ content: 'Build API', status: 'pending' }],
  },
]

// ── parseTodoTags ──

describe('parseTodoTags', () => {
  it('parses self-closing tags', () => {
    const tags = parseTodoTags('Hello\n<todo:start seq="1-3" />\nWorld')
    expect(tags).toHaveLength(1)
    expect(tags[0]).toEqual({ op: 'start', seq: '1-3' })
  })

  it('parses view tag', () => {
    const tags = parseTodoTags('<todo:view />')
    expect(tags).toHaveLength(1)
    expect(tags[0]).toEqual({ op: 'view' })
  })

  it('parses init with nested phases', () => {
    const text = `<todo:init>
<todo:phase name="Setup">
<todo:item>Task A</todo:item>
<todo:item>Task B</todo:item>
</todo:phase>
<todo:phase name="Impl">
<todo:item>Task C</todo:item>
</todo:phase>
</todo:init>`
    const tags = parseTodoTags(text)
    expect(tags).toHaveLength(1)
    expect(tags[0]).toEqual({
      op: 'init',
      phases: [
        { name: 'Setup', items: ['Task A', 'Task B'] },
        { name: 'Impl', items: ['Task C'] },
      ],
    })
  })

  it('parses append with items', () => {
    const text = `<todo:append phase="2">
<todo:item>New task</todo:item>
</todo:append>`
    const tags = parseTodoTags(text)
    expect(tags).toHaveLength(1)
    expect(tags[0]).toEqual({ op: 'append', phaseSeq: 2, items: ['New task'] })
  })

  it('parses multiple tags in order', () => {
    const text = `<todo:start seq="1-2" />\nText\n<todo:done seq="1-3" />`
    const tags = parseTodoTags(text)
    expect(tags).toHaveLength(2)
    expect(tags[0]).toEqual({ op: 'start', seq: '1-2' })
    expect(tags[1]).toEqual({ op: 'done', seq: '1-3' })
  })

  it('returns empty for text without tags', () => {
    expect(parseTodoTags('Just regular text')).toEqual([])
  })

  it('ignores malformed tags', () => {
    expect(parseTodoTags('<todo:start >')).toEqual([])
    expect(parseTodoTags('<todo:done seq="1-1"')).toEqual([])
  })
})

// ── resolveSeq ──

describe('resolveSeq', () => {
  it('resolves task-level seq', () => {
    const result = resolveSeq(PHASES, '1-2')
    expect(result?.task?.content).toBe('Configure DB')
    expect(result?.phase?.name).toBe('Setup')
  })

  it('resolves phase-level seq', () => {
    const result = resolveSeq(PHASES, '2')
    expect(result?.task).toBeUndefined()
    expect(result?.phase?.name).toBe('Impl')
  })

  it('returns undefined for out-of-bounds phase', () => {
    expect(resolveSeq(PHASES, '9-1')).toBeUndefined()
  })

  it('returns undefined for out-of-bounds task', () => {
    expect(resolveSeq(PHASES, '1-9')).toBeUndefined()
  })

  it('returns undefined for missing seq', () => {
    expect(resolveSeq(PHASES, undefined)).toBeUndefined()
  })
})

// ── applyTodoTags ──

describe('applyTodoTags', () => {
  it('applies start tag', () => {
    const { phases, errors } = applyTodoTags(PHASES, '<todo:start seq="1-3" />')
    expect(errors).toHaveLength(0)
    const task = phases[0]!.tasks[2]!
    expect(task.status).toBe('in_progress')
  })

  it('applies done tag', () => {
    const { phases, errors } = applyTodoTags(PHASES, '<todo:done seq="1-2" />')
    expect(errors).toHaveLength(0)
    expect(phases[0]!.tasks[1]!.status).toBe('completed')
  })

  it('applies init tag', () => {
    const text = `<todo:init><todo:phase name="New"><todo:item>Task X</todo:item></todo:phase></todo:init>`
    const { phases, errors } = applyTodoTags(PHASES, text)
    expect(errors).toHaveLength(0)
    expect(phases).toHaveLength(1)
    expect(phases[0]!.name).toBe('New')
  })

  it('applies append tag', () => {
    const text = `<todo:append phase="1"><todo:item>Extra task</todo:item></todo:append>`
    const { phases, errors } = applyTodoTags(PHASES, text)
    expect(errors).toHaveLength(0)
    expect(phases[0]!.tasks).toHaveLength(4)
    expect(phases[0]!.tasks[3]!.content).toBe('Extra task')
  })

  it('applies multiple tags sequentially', () => {
    const text = '<todo:done seq="1-2" />\n<todo:done seq="1-3" />'
    const { phases, errors } = applyTodoTags(PHASES, text)
    expect(errors).toHaveLength(0)
    expect(phases[0]!.tasks[1]!.status).toBe('completed')
    expect(phases[0]!.tasks[2]!.status).toBe('completed')
  })

  it('resolves all seqs against original snapshot', () => {
    // rm shifts indices; both tags resolved against original layout
    const text = '<todo:rm seq="1-1" />\n<todo:done seq="1-3" />'
    const { phases, errors } = applyTodoTags(PHASES, text)
    expect(errors).toHaveLength(0)
    // 'Write tests' was 1-3, now completed
    const writeTests = phases[0]!.tasks.find((t) => t.content === 'Write tests')
    expect(writeTests?.status).toBe('completed')
  })

  it('reports errors for invalid seq', () => {
    const { errors } = applyTodoTags(PHASES, '<todo:done seq="9-9" />')
    expect(errors.length).toBeGreaterThan(0)
  })

  it('detects view tag', () => {
    const { hasView } = applyTodoTags(PHASES, '<todo:view />')
    expect(hasView).toBe(true)
  })

  it('returns no-op for empty text', () => {
    const { phases, errors, hasView } = applyTodoTags(PHASES, 'no tags here')
    expect(errors).toHaveLength(0)
    expect(hasView).toBe(false)
    expect(phases).toEqual(PHASES)
  })
})
```

- [ ] **Step 2: 运行测试验证失败**

Run: `pnpm vitest run src/core/todo-tags.test.ts`
Expected: FAIL — 模块不存在

- [ ] **Step 3: 实现 todo-tags.ts**

创建 `src/core/todo-tags.ts`：

```typescript
// todo-tags: parse <todo:*> tags from assistant text and apply them to todo phases.
// Acts as a format-conversion layer on top of todo.ts's applyParams state machine.
// Tags are NOT stripped from the text — they remain visible to the user.

import {
  applyParams,
  clonePhases,
  type TodoInput,
  type TodoItem,
  type TodoPhase,
} from '../tools/builtin/todo.js'

// =============================================================================
// Types
// =============================================================================

/** A parsed todo tag, before seq resolution. */
type ParsedTodoTag =
  | { op: 'init'; phases: { name: string; items: string[] }[] }
  | { op: 'start'; seq: string | undefined }
  | { op: 'done'; seq: string | undefined }
  | { op: 'drop'; seq: string | undefined }
  | { op: 'rm'; seq: string | undefined }
  | { op: 'append'; phaseSeq: number; items: string[] }
  | { op: 'view' }

/** Result of applying todo tags to phases. */
type ApplyTodoTagsResult = {
  phases: TodoPhase[]
  errors: string[]
  hasView: boolean
}

// =============================================================================
// Tag parser
// =============================================================================

/** Unified regex matching self-closing and container todo tags.
 *  Group 1 = tag name, Group 2 = attributes, Group 3 = inner content (undefined for self-closing).
 *  Backreference \1 ensures container close tag matches open tag. */
const TAG_RE = /<todo:(\w+)([^>]*?)(?:\/>|>([\s\S]*?)<\/todo:\1>)/g

/** Parse <todo:item> tags from inner content. */
function parseItems(inner: string): string[] {
  const items: string[] = []
  const itemRe = /<todo:item>([\s\S]*?)<\/todo:item>/g
  for (const m of inner.matchAll(itemRe)) {
    items.push(m[1]!.trim())
  }
  return items
}

/** Parse <todo:phase> blocks from init inner content. */
function parseInitInner(inner: string): { name: string; items: string[] }[] {
  const phases: { name: string; items: string[] }[] = []
  const phaseRe = /<todo:phase\s+name="([^"]+)">([\s\S]*?)<\/todo:phase>/g
  for (const m of inner.matchAll(phaseRe)) {
    phases.push({ name: m[1]!.trim(), items: parseItems(m[2] ?? '') })
  }
  // Flat items without phase wrapper → default phase
  if (phases.length === 0) {
    const flat = parseItems(inner)
    if (flat.length > 0) phases.push({ name: 'Tasks', items: flat })
  }
  return phases
}

/** Parse all todo tags from text, preserving document order. */
export function parseTodoTags(text: string): ParsedTodoTag[] {
  const tags: ParsedTodoTag[] = []
  for (const m of text.matchAll(TAG_RE)) {
    const op = m[1]!
    const attrs = m[2] ?? ''
    const inner = m[3]

    // Skip structural tags parsed as part of init/append
    if (op === 'phase' || op === 'item') continue

    switch (op) {
      case 'init':
        tags.push({ op: 'init', phases: parseInitInner(inner ?? '') })
        break
      case 'append': {
        const phaseMatch = /phase="([^"]+)"/.exec(attrs)
        const phaseSeq = phaseMatch ? Number.parseInt(phaseMatch[1]!, 10) : Number.NaN
        tags.push({ op: 'append', phaseSeq, items: parseItems(inner ?? '') })
        break
      }
      case 'start':
      case 'done':
      case 'drop':
      case 'rm': {
        const seqMatch = /seq="([^"]+)"/.exec(attrs)
        tags.push({ op, seq: seqMatch?.[1]?.trim() })
        break
      }
      case 'view':
        tags.push({ op: 'view' })
        break
      // Unknown tag names are silently ignored
    }
  }
  return tags
}

// =============================================================================
// Seq resolver
// =============================================================================

/** Resolve a seq string ("1-2" or "1") to a phase and/or task.
 *  Returns undefined if out of bounds or unparseable. */
export function resolveSeq(
  phases: TodoPhase[],
  seq: string | undefined,
): { phase: TodoPhase; task?: TodoItem } | undefined {
  if (!seq) return undefined
  const parts = seq.split('-').map((s) => Number.parseInt(s.trim(), 10))
  if (parts.length === 0 || Number.isNaN(parts[0]!)) return undefined

  const phaseIdx = parts[0]! - 1
  if (phaseIdx < 0 || phaseIdx >= phases.length) return undefined
  const phase = phases[phaseIdx]!

  if (parts.length === 1) return { phase }

  if (Number.isNaN(parts[1]!)) return undefined
  const taskIdx = parts[1]! - 1
  if (taskIdx < 0 || taskIdx >= phase.tasks.length) return undefined
  return { phase, task: phase.tasks[taskIdx] }
}

// =============================================================================
// Tag → TodoInput converter
// =============================================================================

/** Convert a parsed tag to a TodoInput, resolving seq against the given phases snapshot. */
function tagToTodoInput(
  phases: TodoPhase[],
  tag: ParsedTodoTag,
): { input?: TodoInput; error?: string } {
  switch (tag.op) {
    case 'init':
      return {
        input: {
          op: 'init',
          list: tag.phases.map((p) => ({ phase: p.name, items: p.items })),
        },
      }
    case 'append':
      if (Number.isNaN(tag.phaseSeq) || tag.phaseSeq < 1 || tag.phaseSeq > phases.length) {
        return { error: `Phase ${tag.phaseSeq} not found (have ${phases.length} phases)` }
      }
      return {
        input: {
          op: 'append',
          phase: phases[tag.phaseSeq - 1]!.name,
          items: tag.items,
        },
      }
    case 'view':
      return { input: { op: 'view' } }
    case 'start': {
      const resolved = resolveSeq(phases, tag.seq)
      if (!resolved) return { error: `Invalid seq "${tag.seq}"` }
      if (!resolved.task)
        return { error: 'start requires a task-level seq (e.g. "1-2"), not phase-level' }
      return { input: { op: 'start', task: resolved.task.content } }
    }
    case 'done':
    case 'drop':
    case 'rm': {
      const resolved = resolveSeq(phases, tag.seq)
      if (!resolved) return { error: `Invalid seq "${tag.seq}"` }
      if (!resolved.task) return { input: { op: tag.op, phase: resolved.phase.name } }
      return { input: { op: tag.op, task: resolved.task.content } }
    }
  }
}

// =============================================================================
// Main entry: applyTodoTags
// =============================================================================

/** Parse todo tags from text, resolve all seqs against the original snapshot,
 *  then apply sequentially. Tags are applied in document order.
 *  Does NOT mutate the input phases (clones first). */
export function applyTodoTags(phases: TodoPhase[], text: string): ApplyTodoTagsResult {
  const tags = parseTodoTags(text)
  if (tags.length === 0) return { phases, errors: [], hasView: false }

  // Pre-resolve all seqs against the original snapshot (LLM writes all tags
  // based on one view; rm shifting must not corrupt later references).
  const inputs: TodoInput[] = []
  const errors: string[] = []
  let hasView = false

  for (const tag of tags) {
    if (tag.op === 'view') {
      hasView = true
      continue
    }
    const { input, error } = tagToTodoInput(phases, tag)
    if (error) errors.push(error)
    else if (input) inputs.push(input)
  }

  // Apply sequentially on a clone
  let current = clonePhases(phases)
  for (const input of inputs) {
    const result = applyParams(current, input)
    if (result.errors.length > 0) {
      errors.push(...result.errors)
    } else {
      current = result.phases
    }
  }

  return { phases: current, errors, hasView }
}
```

- [ ] **Step 4: 运行测试验证通过**

Run: `pnpm vitest run src/core/todo-tags.test.ts`
Expected: PASS — 全部测试通过

- [ ] **Step 5: 运行类型检查**

Run: `pnpm tsc --noEmit`
Expected: 无错误

- [ ] **Step 6: Commit**

```bash
git add src/core/todo-tags.ts src/core/todo-tags.test.ts
git commit -m "feat(todo-tags): add tag parser, seq resolver, and applyTodoTags"
```

---

## Task 3: AgentEvent 类型加 todo_update

**Files:**
- Modify: `src/shared/types/agent.ts`

- [ ] **Step 1: 添加 todo_update 到 AgentEvent 联合类型**

在 `src/shared/types/agent.ts` 的 `AgentEvent` 类型中，在 `{ _tag: 'done' }` 之前添加：

```typescript
  /** Tag-based todo 操作成功后发射，前端据此刷新 TodoPanel。 */
  | { _tag: 'todo_update'; phases: { name: string; tasks: { content: string; status: string }[] }[] }
```

- [ ] **Step 2: 运行类型检查**

Run: `pnpm tsc --noEmit`
Expected: 无错误（AgentEvent 是类型定义，不影响运行时）

- [ ] **Step 3: Commit**

```bash
git add src/shared/types/agent.ts
git commit -m "feat(agent): add todo_update event to AgentEvent"
```

---

## Task 4: loop.ts — 集成 tag 处理

**Files:**
- Modify: `src/core/loop.ts`
- Test: `src/core/loop.test.ts`

- [ ] **Step 1: 写集成失败测试**

在 `src/core/loop.test.ts` 的顶层 describe 块末尾添加：

```typescript
  // ── todo tag processing ──

  it('processes <todo:*> tags from assistant text and updates state', async () => {
    const db = await createDB({ driver: 'pglite' })
    await migrateDB(db)
    const session = await createSession(db, { cwd: process.cwd() })
    const messages = await getMessages(db, session.id)

    const registry = createRegistry()
    registerProvider(registry, {
      providerId: 'test',
      async chatStream() {
        async function* gen() {
          yield {
            _tag: 'text',
            text: 'Let me set up tasks.\n<todo:init><todo:phase name="Work"><todo:item>Task A</todo:item><todo:item>Task B</todo:item></todo:phase></todo:init>',
          } as const
          yield { _tag: 'done' } as const
        }
        return gen()
      },
    })

    const state = makeState(session, messages)
    state.config = { ...state.config, provider: 'test', model: 'm1', tools: [] }
    const deps: LoopDeps = {
      db,
      cwd: process.cwd(),
      toolRegistry: createDefaultRegistry(createToolRegistry([])),
      permission: { check: async () => ({ ok: true }) },
      llmRegistry: registry,
      hookRunner: null,
      agentRegistry: undefined,
    }

    const events: AgentEvent[] = []
    for await (const ev of agentLoop(state, deps)) {
      events.push(ev)
    }

    // state.todoPhases should be updated
    expect(state.todoPhases).toHaveLength(1)
    expect(state.todoPhases[0]!.name).toBe('Work')
    expect(state.todoPhases[0]!.tasks).toHaveLength(2)

    // todo_update event should be emitted
    const todoUpdate = events.find((e) => e._tag === 'todo_update')
    expect(todoUpdate).toBeTruthy()
    if (todoUpdate && todoUpdate._tag === 'todo_update') {
      expect(todoUpdate.phases).toHaveLength(1)
      expect(todoUpdate.phases[0]!.tasks).toHaveLength(2)
    }
  })

  it('injects steering on tag error', async () => {
    const db = await createDB({ driver: 'pglite' })
    await migrateDB(db)
    const session = await createSession(db, { cwd: process.cwd() })
    const messages = await getMessages(db, session.id)

    // Pre-seed todo state
    state.todoPhases = [
      { name: 'Setup', tasks: [{ content: 'Task A', status: 'pending' }] },
    ]

    const registry = createRegistry()
    registerProvider(registry, {
      providerId: 'test',
      async chatStream() {
        async function* gen() {
          yield { _tag: 'text', text: '<todo:done seq="9-9" />' } as const
          yield { _tag: 'done' } as const
        }
        return gen()
      },
    })

    const state = makeState(session, messages)
    state.config = { ...state.config, provider: 'test', model: 'm1', tools: [] }
    state.todoPhases = [
      { name: 'Setup', tasks: [{ content: 'Task A', status: 'pending' }] },
    ]
    const deps: LoopDeps = {
      db,
      cwd: process.cwd(),
      toolRegistry: createDefaultRegistry(createToolRegistry([])),
      permission: { check: async () => ({ ok: true }) },
      llmRegistry: registry,
      hookRunner: null,
      agentRegistry: undefined,
    }

    for await (const _ev of agentLoop(state, deps)) {
      // drain
    }

    // Steering queue should have an error message
    expect(state.steeringQueue.length).toBeGreaterThan(0)
    expect(state.steeringQueue[0]).toContain('Invalid seq')
  })
```

- [ ] **Step 2: 运行测试验证失败**

Run: `pnpm vitest run src/core/loop.test.ts -t "todo"`
Expected: FAIL — tag 不被处理，todoPhases 不更新

- [ ] **Step 3: 在 loop.ts 添加导入**

在 `src/core/loop.ts` 的 import 区域添加：

```typescript
import { applyTodoTags } from './todo-tags.js'
import { formatSummary } from '../tools/builtin/todo.js'
import { injectSteering } from './steering.js'
```

注意：`injectSteering` 当前已有定义在同文件中使用的 steering.ts 导入路径——检查是否已经导入。如果没有，添加上述导入。如果 `drainSteering` 已被导入，在同一 import 语句中添加 `injectSteering`。

- [ ] **Step 4: 实现 processTodoTags 生成器函数**

在 `src/core/loop.ts` 的 `persistAssistantAndTools` 函数之后添加：

```typescript
/** Process <todo:*> tags embedded in assistant text.
 *  Parses tags, applies them to state.todoPhases via applyTodoTags,
 *  yields todo_update event on success, injects steering on error/view. */
async function* processTodoTags(
  state: AgentState,
  text: string,
): AsyncGenerator<AgentEvent> {
  const result = applyTodoTags(state.todoPhases as TodoPhase[], text)
  if (result.errors.length > 0) {
    // Inject error feedback into steering queue for next turn
    injectSteering(
      state,
      `<todo-tag-errors>\n${result.errors.join('\n')}\n</todo-tag-errors>`,
    )
  }
  if (result.hasView) {
    // View request: inject current state (with seq) into steering
    const summary = formatSummary(result.phases, [], true)
    injectSteering(state, `<todo-state>\n${summary}\n</todo-state>`)
  }
  // Always update state if phases changed or tags were found
  const tagsFound = result.errors.length > 0 || result.hasView || result.phases !== state.todoPhases
  if (tagsFound) {
    state.todoPhases = result.phases
    yield { _tag: 'todo_update', phases: result.phases }
  }
}
```

注意：需要在文件顶部添加 `TodoPhase` 类型导入（如果尚未导入）：

```typescript
import type { TodoPhase } from '../tools/builtin/todo.js'
```

可以与 `formatSummary` 的导入合并：

```typescript
import { formatSummary, type TodoPhase } from '../tools/builtin/todo.js'
```

- [ ] **Step 5: 在 agentLoop 中调用 processTodoTags**

在 `agentLoop` 函数中，找到调用 `persistAssistantAndTools` 的位置：

```typescript
    // —— 持久化 assistant + 执行工具 + 透传 tool_call/subagent 事件 ——
    yield* persistAssistantAndTools(state, deps, collected.text, validCalls)
```

在其之后（`if (validToolCalls.length === 0)` 之前）添加：

```typescript
    // —— 处理 assistant 文本中的 <todo:*> 标签 ——
    yield* processTodoTags(state, collected.text.join(''))
```

- [ ] **Step 6: 运行测试验证通过**

Run: `pnpm vitest run src/core/loop.test.ts -t "todo"`
Expected: PASS

- [ ] **Step 7: 运行完整 loop 测试确认无回归**

Run: `pnpm vitest run src/core/loop.test.ts`
Expected: PASS — 全部测试通过

- [ ] **Step 8: 运行类型检查**

Run: `pnpm tsc --noEmit`
Expected: 无错误

- [ ] **Step 9: Commit**

```bash
git add src/core/loop.ts src/core/loop.test.ts
git commit -m "feat(loop): integrate todo tag processing into agent loop"
```

---

## Task 5: Server route + Frontend — steering 注入 + todo_update 处理

**Files:**
- Modify: `src/server/routes/todo.ts`
- Modify: `src/web/hooks/useChat.ts`

- [ ] **Step 1: 修改 server route 注入 steering**

在 `src/server/routes/todo.ts` 顶部添加导入：

```typescript
import { formatSummary, type TodoPhase } from '../../tools/builtin/todo.js'
```

在 POST handler 中，找到更新 `run.state.todoPhases` 的位置：

```typescript
      if (run) {
        run.state.todoPhases = updatedPhases
      }
```

替换为：

```typescript
      if (run) {
        run.state.todoPhases = updatedPhases
        // 前端手动修改了 todo 状态 → 注入 steering 通知 LLM（通道 A）
        const summary = formatSummary(updatedPhases, [], true)
        run.state.steeringQueue.push(
          `<todo-state-external>\nA todo change was made externally (via UI).\n${summary}\n</todo-state-external>`,
        )
      }
```

- [ ] **Step 2: 修改 useChat.ts 处理 todo_update 事件**

在 `src/web/hooks/useChat.ts` 的 `reduceChatEvent` 函数中，找到最后的 `default` case 之前，添加 `todo_update` case。

首先，在文件顶部找到已有的 `useQueryClient` 或 `qc` 引用——`todo_update` 在 `reduceChatEvent` 中不做状态变更（因为 TodoPanel 用 React Query 独立获取），而是在 SSE 事件回调中 invalidate query。

在 `doStream` 的 SSE 事件回调中，找到处理 `llm_detail` 的条件块：

```typescript
            if (event._tag === 'llm_detail') {
              if (llmDetailTimerRef.current) clearTimeout(llmDetailTimerRef.current)
              llmDetailTimerRef.current = setTimeout(() => {
                qc.invalidateQueries({ queryKey: ['session', sessionId, 'llm-details'] })
              }, 500)
            } else if (event._tag === 'done' || event._tag === 'error') {
```

在 `llm_detail` 块之后、`else if` 之前添加：

```typescript
            } else if (event._tag === 'todo_update') {
              qc.invalidateQueries({ queryKey: ['todo', sessionId] })
```

这样 `todo_update` 事件触发 TodoPanel 的 React Query 重新获取。

注意：`reduceChatEvent` 不需要处理 `todo_update`——它是纯 UI 刷新事件，不改变聊天消息状态。`reduceChatEvent` 的 switch 中没有匹配 case 会走到 default（no-op），这是安全的。

- [ ] **Step 3: 运行类型检查**

Run: `pnpm tsc --noEmit`
Expected: 无错误

- [ ] **Step 4: 运行前端测试确认无回归**

Run: `pnpm vitest run src/web/hooks/useChat.test.ts`
Expected: PASS

- [ ] **Step 5: 运行 server 测试确认无回归**

Run: `pnpm vitest run src/server/`
Expected: PASS

- [ ] **Step 6: Commit**

```bash
git add src/server/routes/todo.ts src/web/hooks/useChat.ts
git commit -m "feat(todo): inject steering on manual UI ops, handle todo_update in frontend"
```

---

## Task 6: Prompt registry — 文档化 tag 语法

**Files:**
- Modify: `src/core/prompt-registry.ts`

- [ ] **Step 1: 添加 tag 语法文档到 EXECUTION_WORKFLOW**

在 `src/core/prompt-registry.ts` 中，找到 `EXECUTION_WORKFLOW` 常量。

在步骤 3（Decompose）之后：

```typescript
3. Decompose — break multi-step work into steps and track them with the \`todo\` tool; skip for trivial requests. Plan only what makes the request work.
```

替换为包含 tag 语法的版本：

```typescript
3. Decompose — break multi-step work into steps and track them with the \`todo\` tool; skip for trivial requests. Plan only what makes the request work.

You can also manage todos inline via tags in your text output (saves a tool-call round-trip). Tags use \`phase-task\` sequence numbers visible in the todo summary (e.g. \`1-2\` = phase 1, task 2; \`1\` = all tasks in phase 1). Operations:
  - \`<todo:init><todo:phase name="Name"><todo:item>Task</todo:item></todo:phase></todo:init>\` — create/replace the list
  - \`<todo:start seq="1-1" />\` — mark task in progress (task-level seq only)
  - \`<todo:done seq="1-2" />\` — complete a task (or entire phase: \`seq="1"\`)
  - \`<todo:drop seq="2-1" />\` — abandon a task
  - \`<todo:rm seq="1-1" />\` — remove a task
  - \`<todo:append phase="1"><todo:item>New</todo:item></todo:append>\` — add tasks (phase is 1-based index)
  - \`<todo:view />\` — request current state
Tags remain visible in your output text; they are not stripped. Both tags and the todo tool update the same state — use whichever is more convenient.
```

- [ ] **Step 2: 运行类型检查**

Run: `pnpm tsc --noEmit`
Expected: 无错误

- [ ] **Step 3: 运行 prompt registry 测试**

Run: `pnpm vitest run src/core/prompt-registry --passWithNoTests`
Expected: PASS 或无测试（不报错）

- [ ] **Step 4: Commit**

```bash
git add src/core/prompt-registry.ts
git commit -m "docs(prompt): document <todo:*> tag syntax in system prompt"
```

---

## Task 7: 全量验证

- [ ] **Step 1: 运行全量类型检查**

Run: `pnpm tsc --noEmit`
Expected: 无错误

- [ ] **Step 2: 运行全量测试**

Run: `pnpm vitest run`
Expected: 全部 PASS

- [ ] **Step 3: Biome 格式检查**

Run: `pnpm biome check src/core/todo-tags.ts src/core/todo-tags.test.ts src/core/loop.ts src/tools/builtin/todo.ts src/server/routes/todo.ts src/web/hooks/useChat.ts src/core/prompt-registry.ts src/shared/types/agent.ts`
Expected: 无格式问题（如有，运行 `pnpm biome check --write` 修复）

---

## Self-Review

**1. Spec coverage:**
- ✅ Tag 协议（全 7 op 覆盖）：Task 2 parser 支持 init/start/done/drop/rm/append/view
- ✅ 标签名 `todo:`：Task 2 TAG_RE 使用 `todo:` 前缀
- ✅ seq 格式 `phase-task`：Task 2 resolveSeq 实现
- ✅ seq 不在前端展示：Task 1 formatSummary 输出仅 LLM 可见；Task 5 useChat 只 invalidate query
- ✅ 保留 tag 不 strip：Task 4 processTodoTags 不修改文本
- ✅ 反馈通道 B（LLM 成功）：Task 4 yield todo_update
- ✅ 反馈通道 A（错误/view/前端手动）：Task 4 steering inject；Task 5 server route steering inject
- ✅ 复用 applyParams：Task 1 导出；Task 2 applyTodoTags 调用
- ✅ prompt 文档：Task 6

**2. Placeholder scan:** 无 TBD/TODO，每步有完整代码。

**3. Type consistency:**
- `ParsedTodoTag` 在 Task 2 定义，Task 4 通过 `applyTodoTags` 间接使用
- `TodoPhase` 从 todo.ts 导入，在 todo-tags.ts 和 loop.ts 中使用
- `applyParams` 在 Task 1 导出，Task 2 导入
- `formatSummary` 在 Task 1 导出，Task 4 和 Task 5 导入
- `todo_update` event 在 Task 3 定义，Task 4 yield，Task 5 前端处理
- `injectSteering` 在 Task 4 使用（已存在于 steering.ts）

# 开发环境隔离：把 dev server 的全局配置/工作流/插件（C0DE_CONFIG_DIR）
# 与运行时数据 DB/设备认证（C0DE_DB_DIR）从安装版默认位置
# （~/.c0de、~/.local/share/c0de/pglite）重定向到仓库内 .c0de-dev/。
# 相对路径按进程 cwd（仓库根）解析。复制为 .env 后生效（.env 不入 git）。
C0DE_CONFIG_DIR=.c0de-dev
C0DE_DB_DIR=.c0de-dev/pglite
